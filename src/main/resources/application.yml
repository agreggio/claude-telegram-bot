claude-bot:
  bot-token: ${TELEGRAM_BOT_TOKEN}
  chat-id: ${TELEGRAM_CHAT_ID}
  working-directory: ${user.home}
  claude-path: ${CLAUDE_PATH:claude}
  timeout-seconds: ${CLAUDE_TIMEOUT:120}
  download-path: ${DOWNLOAD_PATH:${user.home}/Downloads/telegram}

spring:
  main:
    banner-mode: off
